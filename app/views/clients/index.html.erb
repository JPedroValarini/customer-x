<div class="table-responsive">
  <div class="container">
    <table class="table">
      <thead class="thead-dark">
        <tr>
          <th scope="col">Full name</th>
          <th scope="col">Email</th>
          <th scope="col">Telephone</th>
          <th scope="col">Date register</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @clients.each do |client| %>
          <tr>
            <td><%= client.full_name %></td>
            <td><%= client.email %></td>
            <td><%= client.telephone %></td>
            <td><%= client.date_register %></td>
            <td><%= link_to 'Show', client, :class => "btn btn-success" %></td>
            <td><%= link_to 'Edit', edit_client_path(client), :class => "btn btn-warning" %></td>
            <td><%= link_to 'Destroy', client, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <small class="new_client" ><%= link_to 'New Client', new_client_path, :class => 'btn btn-dark' %></small>
  </div>
</div>

<br>


